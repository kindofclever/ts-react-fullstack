{
  "buildCommand": "rm -rf build/ && tsc",
  "outputDirectory": "build",
  "version": 2,
  "builds": [
    {
      "src": "./src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "@vercel/node"
    }
  ]
}